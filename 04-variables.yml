- name: variables
  hosts: localhost
  vars:
   Course: "ansible"
   Trainer: "sivakumar"
   Duration: "90hrs"
  tasks:
  - name: print info
    ansible.builtin.debug:
     msg: "hi, we are learning {{Course}} from Trainer {{Trainer}}, duration is {{Duration}}